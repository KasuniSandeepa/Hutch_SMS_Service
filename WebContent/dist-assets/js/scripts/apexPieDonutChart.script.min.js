"use strict";
var y= parseInt(document.getElementById("PhoneNumber01").innerHTML);
var x= parseInt(document.getElementById("PhoneNumber02").innerHTML);

//var tot= x+y;

//var pnum01 = x/(x+y) *100;
//var pnum02 = y/(x+y) *100;
$(document).ready(function () {
  // simple Pie
  var options = {
    chart: {
      width: '100%',
      type: 'pie'
    },
    labels: ['Phone Number 02', 'Phone Number 01'],
    series: [x,y],
    legend: {
      position: 'bottom'
    },
    responsive: [{
      breakpoint: 480,
      options: {
        chart: {
          width: 300
        },
        legend: {
          position: 'bottom',
          offsetY: 40
        }
      }
    }]
  };
  var chart = new ApexCharts(document.querySelector("#simplePie"), options);
  chart.render(); // simple donut

  var options = {
    chart: {
      type: 'donut',
      width: '100%'
    },
    series: [x, y],
    legend: {
      position: 'bottom'
    },
    responsive: [{
      breakpoint: 480,
      options: {
        chart: {
          width: 310
        },
        legend: {
          position: 'bottom'
        }
      }
    }]
  };
 
});